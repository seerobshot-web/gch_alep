import { PageHero } from '../components/PageHero';

export default function LoginPage() {
  return (
    <PageHero
      eyebrow="Sign In"
      title="Sign in to your account."
      description="Use the email and password you created at checkout to manage your services, invoices, and support tickets."
    />
  );
}
