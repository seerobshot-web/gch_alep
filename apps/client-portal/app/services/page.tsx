import { PageHero } from '../components/PageHero';

export default function ServicesPage() {
  return (
    <PageHero
      eyebrow="Services"
      title="Your active services."
      description="Every hosting plan, VPS, and subscription on your account, with status, renewal date, and management links."
    />
  );
}
