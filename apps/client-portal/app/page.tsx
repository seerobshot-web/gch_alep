import { PageHero } from './components/PageHero';

export default function DashboardPage() {
  return (
    <PageHero
      eyebrow="Dashboard"
      title="Welcome back."
      description="A snapshot of your active services, upcoming renewals, and any open support tickets — all in one place."
    />
  );
}
