import { PageHero } from '../components/PageHero';

export default function PortalSupportPage() {
  return (
    <PageHero
      eyebrow="Support"
      title="Open and track support tickets."
      description="Start a new ticket or follow up on an existing one — our team reads every message and replies personally."
    />
  );
}
