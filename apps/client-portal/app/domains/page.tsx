import { PageHero } from '../components/PageHero';

export default function PortalDomainsPage() {
  return (
    <PageHero
      eyebrow="Domains"
      title="Your domains."
      description="Renewal dates, nameservers, and contact details for every domain registered through Glory Cloud Host."
    />
  );
}
