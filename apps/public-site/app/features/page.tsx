import { PageHero } from '../components/PageHero';

export default function FeaturesPage() {
  return (
    <PageHero
      eyebrow="Features"
      title="Infrastructure you can lean on."
      description="Automated provisioning, monitored uptime, free migration, and a support team that knows your account — everything your ministry's web presence stands on."
    />
  );
}
