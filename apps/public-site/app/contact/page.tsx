import { PageHero } from '../components/PageHero';

export default function ContactPage() {
  return (
    <PageHero
      eyebrow="Contact"
      title="Have a question before you sign up?"
      description="Reach our team directly — we'd rather answer it now than have you guess after checkout."
    />
  );
}
