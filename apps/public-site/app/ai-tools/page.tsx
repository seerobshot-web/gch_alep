import { PageHero } from '../components/PageHero';

export default function AiToolsPage() {
  return (
    <PageHero
      eyebrow="AI Tools"
      title="AI utilities, curated and provisioned for you."
      description="A vetted lineup of AI tools your team can subscribe to in minutes, billed alongside everything else you already host with us."
    />
  );
}
