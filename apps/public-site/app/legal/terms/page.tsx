import { PageHero } from '../../components/PageHero';

export default function TermsPage() {
  return (
    <PageHero
      eyebrow="Legal"
      title="Terms of Service"
      description="The agreement that governs your use of Glory Cloud Host's hosting, domain, and add-on services — full text to follow."
    />
  );
}
