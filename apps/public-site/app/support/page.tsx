import { PageHero } from '../components/PageHero';

export default function SupportPage() {
  return (
    <PageHero
      eyebrow="Support"
      title="Real answers from people who host your services."
      description="Open a ticket from your client portal and hear back from a human who actually understands cPanel, VPS, and your account — not a script."
    />
  );
}
