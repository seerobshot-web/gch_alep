import { PageHero } from '../components/PageHero';

export default function FaithPage() {
  return (
    <PageHero
      eyebrow="Our Faith"
      title="We build servers the way we try to build everything else — with integrity."
      description="Glory Cloud Host is run on the conviction that how we host, bill, and support our customers should reflect the same faith that gives our company its name."
    />
  );
}
