import { PageHero } from '../components/PageHero';

export default function PricingPage() {
  return (
    <PageHero
      eyebrow="Pricing"
      title="Straightforward pricing, no surprise renewals."
      description="Every plan across hosting, VPS, domains, and add-on tools — one transparent price list, updated live from our billing system."
    />
  );
}
