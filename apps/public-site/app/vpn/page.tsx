import { PageHero } from '../components/PageHero';

export default function VpnPage() {
  return (
    <PageHero
      eyebrow="VPN"
      title="Private access for a team that works from everywhere."
      description="Encrypted, always-on VPN service for staff and volunteers connecting to shared systems from anywhere in the world."
    />
  );
}
