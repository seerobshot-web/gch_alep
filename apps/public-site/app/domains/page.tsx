import { PageHero } from '../components/PageHero';

export default function DomainsPage() {
  return (
    <PageHero
      eyebrow="Domains"
      title="Claim the address your community will remember."
      description="Search, register, and manage domains alongside your hosting — one invoice, one login, one place to renew."
    />
  );
}
