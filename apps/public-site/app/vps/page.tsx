import { PageHero } from '../components/PageHero';

export default function VpsPage() {
  return (
    <PageHero
      eyebrow="VPS Hosting"
      title="Root-access servers for teams ready to scale."
      description="Dedicated CPU, memory, and storage on a virtual private server you fully control, backed by a support team that answers."
    />
  );
}
